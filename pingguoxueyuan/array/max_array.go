package main

import "fmt"

func maxArray() {
	arr1 := []int{1, 2, 3, 4}
	subs := [][]int{}
	for i := 0; i < len(arr1); i++ {
		//for j := i +1 ; j < len(arr1); j++ {
		//	arr2 := append(arr2, )
		//}
		n = i + 1
		if n <= len(arr1) {
			sub = arr1[:]
			subs = append(subs)
		}
	}
	fmt.Println()
}

func main() {
	maxArray()
}
